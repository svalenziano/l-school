- Is this print_blank() function best practice?  My intent is to make the code easier to read, though I realize maybe I'm going too far?
- 


### Pylint
Pylint throws C0103 because it sees the 'round_winner' variable as a constant.  I'm confused by this because the variable will change values with each while loop, so it shouldn't be considered a constant?
